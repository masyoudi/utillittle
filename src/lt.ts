/**
 * Checks if `left` is less than `right`
 * @param {*} left
 * @param {*} right
 * @returns {Boolean}
 */
export function lt(left: any, right: any): boolean {
  return left < right;
}

export default lt;
