/**
 * Checks if `left` is less than or equal `right`
 * @param {*} left
 * @param {*} right
 * @returns {Boolean}
 */
export default function lte(left: any, right: any): boolean {
  return left <= right;
}
