/**
 * Checks if `left` is greater than or equal `right`
 * @param {*} left
 * @param {*} right
 * @returns {Boolean}
 */
export function gte(left: any, right: any): boolean {
  return left >= right;
}

export default gte;
