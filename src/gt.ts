/**
 * Checks if `left` is greater than `right`
 * @param {*} left
 * @param {*} right
 * @returns {Boolean}
 */
export function gt(left: any, right: any): boolean {
  return left > right;
}

export default gt;
