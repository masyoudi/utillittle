/**
 * Checks if `left` is less than or equal `right`
 * @param {*} left
 * @param {*} right
 * @returns {Boolean}
 */
export function lte(left, right) {
    return left <= right;
}
export default lte;
