/**
 * Converts `value` to `string` / using plain `toString`
 * @param {*} value The value to convert.
 * @returns {string} Returns string.
 * @example
 *
 * toChar(1);
 * // => "1"
 *
 * toChar(null);
 * // => ""
 *
 * toChar(undefined);
 * // => ""
 */
export declare function toChar(value: any): string;
export default toChar;
