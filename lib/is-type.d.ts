/**
 * Checks if `value` is type of `typ`
 * @param {*} value
 * @param {String} typ
 * @returns {Boolean}
 */
export declare function isType(value: any, typ: string): boolean;
export default isType;
