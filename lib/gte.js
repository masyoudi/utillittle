/**
 * Checks if `left` is greater than or equal `right`
 * @param {*} left
 * @param {*} right
 * @returns {Boolean}
 */
export function gte(left, right) {
    return left >= right;
}
export default gte;
