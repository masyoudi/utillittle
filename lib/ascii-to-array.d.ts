/**
 * Converts an ASCII `value` to an array.
 * @param {string} value The value to convert.
 * @returns {Array} Returns the converted array.
 * @example
 *
 * asciiToArray("123");
 * // => ["1", "2", "3"]
 *
 * asciiToArray("abc");
 * // => ["a", "b", "c"]
 */
export declare function asciiToArray(value: string): string[];
export default asciiToArray;
