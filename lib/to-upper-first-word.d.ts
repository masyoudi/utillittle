/**
 * Converts every first character of word in `value` to upper case.
 * @param {String} value
 * @returns {String}
 */
export declare function toUpperFirstWord(value: string): string;
export default toUpperFirstWord;
