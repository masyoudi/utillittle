/**
 * Checks if `value` is in `options` (`options` can be `Object` or `Array`)
 * @param {String} value
 * @param {*} options
 * @returns {Boolean}
 */
export declare function isIn(value: string, options: any): boolean;
export default isIn;
